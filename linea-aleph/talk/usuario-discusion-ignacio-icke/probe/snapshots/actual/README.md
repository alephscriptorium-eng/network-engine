# Snapshot actual

- **oldid:** (pendiente — `fetch_snapshot.py --latest`)
- **estado:** revisión vigente en Wikipedia
- **URL:** https://es.wikipedia.org/wiki/Usuario_discusión:Ignacio_Icke

Tras `python3 scripts/fetch_snapshot.py --latest --title "Usuario discusión:Ignacio Icke"`:

```
cache/snapshots/<oldid>.wikitext   # fuente
cache/snapshots/<oldid>.meta.json  # metadatos de fetch
```

Delta SC→hoy: [`snapshots/delta-sc-actual.md`](../delta-sc-actual.md).
